[deploy]
# Web service entrypoint (Nginx + PHP-FPM)
# Worker service: override start command to "/entrypoint-worker.sh" in Railway dashboard
healthcheckPath = "/superadmin/login"
healthcheckTimeout = 30
